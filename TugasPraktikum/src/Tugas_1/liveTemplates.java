package Tugas_1;

public class liveTemplates {
    public double hitungVolumeBola(double r) {
            //r = JAri-jari
            return (4.0 / 3.0) * Math.PI * Math.pow(r, 3);
    }
    public double menghitungVolumeKubus(double s){
        //s = rusuk
        return Math.pow(s,3);
    }

}
